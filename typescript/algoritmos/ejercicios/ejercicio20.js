"use strict";
(function () {
    //Dada una serie de m pares, (color, numero) que corresponden a los tiros de una ruleta, se pide informar: 
    // Cuantas veces salió el numero 0 y, por cada 0, cual fue el numero anterior. 
    // Cuantas veces seguidas llego a repetirse el color negro.
    // El mayor numero de veces seguidas que salieron alternados los colores rojo y negro.
    // El mayor numero de veces seguidas que se nego la segunda docena.
    // Nota: recuerde que la ruleta contiene numeros comprendidos entre 0 y 36. "Negar la segunda docena", significa
    // que el numero en cuestion, no esta comprendido con los numeros 12, y 23. 
})();
